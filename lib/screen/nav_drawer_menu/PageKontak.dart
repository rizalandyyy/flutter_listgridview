// @rizalandy ~ PC

import 'package:flutter/material.dart';

class PageKontak extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kontak'),
        backgroundColor: Colors.brown,
      ),

      body: Center(
        child: Text('Ini page Kontak', style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),),
      ),
    );
  }
}

// @rizalandy ~ PC